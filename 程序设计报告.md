# 程序设计报告

## 全向轮底盘里程计与自主导航系统

---

## 一、项目概述

本项目基于 Ubuntu24.04.3 的 ROS2 Jazzy 构建一套全向轮底盘的里程计。系统通过 USB CDC 串口接收 MCU（STM32）以 200Hz 上报的 IMU 与四轮编码器数据，经 15 维误差状态卡尔曼滤波器（ESKF）融合后输出里程计信息。

**核心数据流：**

```
MCU(STM32) ─USB CDC 200Hz─→ rm_serial_driver ─/tracker/gimbal─→ data_handle_node(ESKF) ─/odom + TF─→ Nav2
```
特别说明的是，本项目采用 Socat 进行 TCP/UDP 数据流转发从而实现远程联调，具体联调相关细节在 `DebugManual.md` 中有说明

---

## 二、代码框架

### 2.1 功能包结构

```
ace_ass/
├── src/
│   ├── rm_interfaces/           # 自定义消息包
│   │   ├── msg/
│   │   │   ├── Gimbal.msg       # 上行：IMU + 四轮角速度 + MCU 时间戳
│   │   │   └── Target.msg       # 下行：通信测试
│   │   ├── CMakeLists.txt
│   │   └── package.xml
│   │
│   ├── serial_driver/           # 串口驱动包
│   │   ├── src/
│   │   │   └── rm_serial_driver.cpp   # 帧解析 + CRC 校验 + 数据发布
│   │   ├── include/
│   │   │   └── rm_serial_driver/
│   │   │       ├── rm_serial_driver.hpp
│   │   │       ├── packet.hpp          # 帧格式定义 + 收发结构体
│   │   │       └── crc.hpp             # CRC16-CCITT 校验
│   │   ├── config/
│   │   │   └── serial_driver.yaml      # 串口设备路径与波特率
│   │   └── launch/
│   │
│   └── my_nav2_robot/          # 导航主包
│       ├── src/
│       │   └── data_handle.cpp        # ESKF 核心算法 (~640行)
│       ├── include/
│       │   └── my_nav2_robot/
│       │       └── data_handle.hpp     # ESKF 类定义
│       ├── config/
│       │   ├── config.yaml             # ESKF 噪声参数
│       │   ├── nav2_params.yaml        # Nav2 全栈参数
│       │   └── nav2_config.rviz        # RViz 可视化配置
│       ├── urdf/
│       │   └── robot.urdf.xacro        # 机器人模型
│       ├── behaviour_trees/
│       │   └── test_nav.xml            # 行为树
│       └── launch/
│           ├── full.launch.py                # 顶层启动
│           └── full_navigation.launch.py     # 导航启动
```

### 2.2 话题拓扑

| 话题 | 消息类型 | 发布者 | 订阅者 |
|------|---------|--------|--------|-----|
| `/tracker/gimbal` | `rm_interfaces/msg/Gimbal` | rm_serial_driver | data_handle_node |
| `/tracker/target` | `rm_interfaces/msg/Target` | data_handle_node | rm_serial_driver |
| `/odom` | `nav_msgs/msg/Odometry` | data_handle_node | Nav2 |
| `/odom_raw` | `nav_msgs/msg/Odometry` | data_handle_node | odom_raw_sub |
| `/path` | `nav_msgs/msg/Path` | data_handle_node | RViz |
| `/path_raw` | `nav_msgs/msg/Path` | data_handle_node | RViz |
| `/wz` | `geometry_msgs/msg/Vector3` | data_handle_node | 调试用 |
| `/bias_acc` | `geometry_msgs/msg/Vector3` | data_handle_node | 调试用 |
| `/bias_gyro` | `geometry_msgs/msg/Vector3` | data_handle_node | 调试用 ｜
| `/acc_compensated` | `geometry_msgs/msg/Vector3` | data_handle_node | 调试用 |
| `/gyro_compensated` | `geometry_msgs/msg/Vector3` | data_handle_node | 调试用 |

### 2.3 启动链

本项目使用 `full.launch.py` 作为顶层启动，通过 composition 加载 `full_navigation.launch.py` 导航启动
```
full.launch.py
├── serial_driver.launch.py        → rm_serial_driver 节点
└── full_navigation.launch.py
    ├── static_transform_publisher  → map → odom
    ├── navigation_launch.py (Nav2) → controller / planner / bt_navigator / behavior
    ├── robot_state_publisher       → URDF 发布
    ├── joint_state_publisher       → 关节状态
    ├── data_handle_node            → ESKF 里程计
    └── rviz2                       → 可视化（可选）
```

---

## 三、已实现的功能

### 3.1 串口通信层（`rm_serial_driver`）

本节点为对开源通信节点包结合电控方面提供的校验算法改进得到
| 功能 | 说明 |
|------|------|
| 帧同步与解析 | 逐字节扫描 `0xA5 0x5A`，固定长度读取 52 字节 |
| CRC16-CCITT 校验 | MSB-first，poly=0x1021，校验失败丢弃并打印警告 |
| 长度字段校验 | 验证 `len == 46`，防止帧错位 |
| 双时间戳系统 | `header.stamp`（ROS 系统时间，TF/Nav2 兼容）+ `t_ms`（MCU 采样时间，ESKF 算 dt） |
| 下行发送 | 订阅 `/tracker/target`，打包发送至 MCU |

**帧格式：**

```
[A5][5A][seq:1][len:1=0x2E][payload:46][CRC16-CCITT:2] = 52 bytes
payload: uint32 t_ms + 4×float wheel_speed + 3×float gyro + 3×float acc + uint16 flags
```

### 3.2 ESKF 里程计（`data_handle_node`）

#### 3.2.1 误差状态向量（15 维）

| 分量 | 索引 | 含义 |
|------|------|------|
| δp | 0-2 | 位置误差（世界系） |
| δv | 3-5 | 速度误差（世界系） |
| δθ | 6-8 | 姿态误差（旋转向量，body-frame 右乘） |
| δb_a | 9-11 | 加速度计零偏误差 |
| δb_g | 12-14 | 陀螺仪零偏误差 |

#### 3.2.2 ESKF 流程（每帧执行）

```
gimbalCallBack(msg)
  ├── 零偏标定（静止 1.5s，累积均值）
  ├── 低通滤波（陀螺 α=1.0，加速度计+轮速 α=1.0）
  ├── predict(dt)          ← IMU 积分 + 协方差传播
  ├── observeWheel()       ← 4 维轮速观测更新
  ├── observeZeroTilt()    ← 2 维零倾斜约束
  ├── injectAndReset()     ← 误差注入名义状态 + 重置
  ├── publishOdometry()    ← /odom + TF(odom→base_footprint) + /path
  └── publishRawOdometry() ← /odom_raw + /path_raw（对照用）
```

**predict：** IMU 零偏补偿 → IMU 系到车体系旋转 → 名义状态前向积分 `(p, v, q)` → 15×15 状态转移矩阵 Fx 构建 → 协方差传播 `P = Fx·P·Fx' + Q`

**observeWheel：** 全向轮正运动学解算车体系 `(vx, vy, vz=0, wz)` → 观测预测 `h(x)` + 雅可比 H(4×15) → Kalman 增益 → 误差状态更新 → Joseph 形式协方差更新

**observeZeroTilt：** 提取名义状态 pitch/roll → 约束为零 → 2 维观测更新 → 修正 δθ_x 和 δθ_y，防止陀螺仪 x/y 轴漂移

**injectAndReset：** δx 注入名义状态 `(p+=δp, v+=δv, q=q*δq, b_a+=δb_a, b_g+=δb_g)` → 重置 δx=0

#### 3.2.3 IMU 外参处理

IMU 安装存在小角度偏差，通过离线标定的旋转矩阵 `R_imu_to_body_` 在 predict 阶段将 IMU 原始帧数据旋转到车体系：

```cpp
acc = R_imu_to_body_ * (acc_filtered_ - b_a_);
w   = R_imu_to_body_ * (gyro_filtered_ - b_g_);
```

#### 3.2.4 全向轮运动学

```cpp
constexpr double kWheel = 0.0815 / (4.0 * 0.7071067811865476);  // r/(4*cos45°)
constexpr double kWz    = 0.0815 / (4.0 * 0.2125);               // r/(4*L)

vx = kWheel * ( w_fl + w_fr + w_rl + w_rr)
vy = kWheel * (-w_fl + w_fr + w_rl - w_rr)
vz = 0   // 零速观测
wz = kWz  * (-w_fl + w_fr - w_rl + w_rr)
```

---

## 四、可优化的地方

### 4.1 ESKF 算法层面

| 问题 | 现状 | 优化方向 |
|------|------|---------|
| yaw 不可观 | observeWheel 角速度观测中 `h_x(3)` 使用当前帧 IMU 测量，导致残差≈0，δb_g_z 无法修正；observeZeroTilt 仅约束 pitch/roll | 将 `h_x(3)` 改为上一帧 predict 使用的角速度，使残差反映零偏漂移；或增加磁力计/视觉 yaw 观测 |
| H(3, 6:8) = 0 | 角速度观测对 δθ 的偏导被忽略（标注"二阶小量"），旋转时残差无法正确分配给 δθ | 推导精确偏导 `∂h_w/∂δθ`，旋转时该值不为零 |
| 离心力未补偿 | 旋转时 IMU 加速度计测到离心力分量，被积分到速度中，通过速度观测残差错误修正 δθ | 在 predict 中补偿 `a_compensated = acc - ω × v - ω × (ω × r_imu)` |
| 零偏仅开机标定一次 | 运行期间零偏持续漂移（温漂等），无在线标定机制 | 利用轮速观测的残差在线修正 b_g；或定期在检测到静止时重新标定 |
| 低通滤波器参数 | 当前 α=1.0（即无滤波），实际噪声环境可能需要适当滤波 | 根据实车数据调整 α，在噪声抑制和动态响应间取平衡 |

### 4.2 串口通信层面

| 问题 | 现状 | 优化方向 |
|------|------|---------|
| 帧头误同步 | 逐字节扫描 `0xA5 0x5A`，若 payload 中出现此序列可能误对齐 | CRC 兜底已足够，但可增加 seq 序号连续性检查 |
| 串口断连恢复 | 递归 `reopenPort()` 无最大重试限制，可能栈溢出 | 改为循环重试，增加最大重试次数 |
| 下行协议简陋 | `SendPacket` 仅含 test 字节，无 CRC | 增加完整帧格式与 CRC 校验 |

### 4.3 Nav2 配置层面

| 问题 | 现状 | 优化方向 |
|------|------|---------|
| 无地图 / AMCL | `map → odom` 静态发布，无定位 | 接入激光雷达 + SLAM 或 AprilTag 定位 |
| 代价图仅有膨胀层 | 无障碍物检测（无传感器数据源） | 接入激光雷达 / 深度相机，添加 obstacle_layer |
| 全局代价图 rolling window | 无静态地图，全局规划退化为局部规划 | 建图后使用 static_layer |

### 4.4 工程层面

| 问题 | 现状 | 优化方向 |
|------|------|---------|
| IMU 外参硬编码 | `R_imu_to_body_` 在构造函数中赋值 | 改为从参数文件加载，支持不同 IMU 安装 |
| 全向轮参数硬编码 | `kWheel`、`kWz` 为 `constexpr` | 改为从参数文件加载，适配不同底盘 |
| `publishRawOdometry` 中 p 用了 ESKF 的 p_ | 第 611-613 行位置取了 `p_` 而非 `p_raw_` | 修正为使用 `p_raw_` |
| path 无上限 | `/path` 的 poses 持续累积 | 取消注释 `if (path_.poses.size() > 5000) path_.poses.clear()` |
| 无单元测试 | 无 colcon test 用例 | 添加 ESKF 数学正确性的单元测试 |

---

## 五、联调记录

### 5.1 联调遇见的问题与解决方法

| 序号 | 问题描述 | 原因分析 | 解决方法 | 结果 |
|------|---------|---------|---------|------|
| 1 | 小车转动时，实际角度总是与理想角度差30% | 运动学解算公式错误，未与电控完整沟通好导致的问题 | 修改运动学解算公式，由电控方提供 | 实际角度与理想角度已不总是相差30%，基本吻合 |
| 2 | 小车转动时，实际角度与理想角度在少数情况下仍然有10～20%的差距 |  | | |
| 3 | | | | |
| 4 | | | | |
| 5 | | | | |
